import{sql}from"drizzle-orm";import{index,integer,sqliteTable,text}from"drizzle-orm/sqlite-core";export const subscriptions=sqliteTable("subscriptions",{id:text("id").primaryKey(),email:text("email").notNull(),favorite:text("favorite").notNull(),country:text("country").notNull(),leadDays:integer("lead_days").notNull().default(14),consent:integer("consent",{mode:"boolean"}).notNull(),status:text("status").notNull().default("pending"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},table=>[index("subscriptions_email_idx").on(table.email,table.status)]);
